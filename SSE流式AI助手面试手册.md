# SSE流式AI助手面试手册

## 1. 使用方式

这份手册分成两层：

- 第一层是速记版，适合面试前快速背诵
- 第二层是展开版，适合把问题、方案、取舍、追问讲透

这次我把表述统一改成了“问题驱动”写法，也就是：

`先遇到什么问题 -> 为什么会出现 -> 我做了什么优化 -> 最终拿到了什么效果`

这样更像真实项目经历，面试里也更有说服力。

---

## 2. 速记版

### 2.1 30秒项目介绍

> 我在这个 React Playground 项目里做了一套流式 AI 助手。最开始只是想把大模型的流式返回接出来，但真正上线到完整聊天场景后，我发现问题不在“能不能流式输出”，而在“长上下文下稳不稳、渲染卡不卡、断线后能不能恢复”。所以我最后做了三类优化：第一类是通信链路优化，从早期的 `EventSource + GET` 演进到 `fetch + POST + ReadableStream`，解决 URL 过长导致的连接问题；第二类是渲染性能优化，用缓冲区做 200ms 批量刷新，避免每个 token 都触发 React 重渲染；第三类是稳定性和交互优化，加入自动重连、断点续传、底部跟随滚动和虚拟滚动，让它更像真实可用的聊天产品。

### 2.2 1分钟选型回答

> 一开始我选 SSE，是因为这个场景本质上是“前端发一次请求，服务端持续往回推结果”，属于典型的单向流式输出，SSE 比 WebSocket 更匹配，也更轻量。后来我遇到的第一个真实问题是：随着上下文、历史消息和文件内容变长，早期的 `EventSource + GET` 会把参数都拼到 URL 里，URL 一长就容易在浏览器或代理层出问题。所以我保留了 SSE 的数据格式和流式语义，但把连接方式演进成了 `fetch + POST + ReadableStream`。这样既保留了流式体验，也解决了参数承载能力和稳定性问题。

### 2.3 1分钟实现回答

> 当前实现里，前端发送消息时会生成一个 `sessionId`，通过 `POST /api/chat-stream` 发给后端。后端会为这条回答建立一个 session，里面缓存原始请求参数、已经生成的 chunk 列表、chunk 的递增序号、状态和当前活跃连接。这样设计是因为我后来遇到一个问题：如果回答生成到一半断线了，单纯重连并不能保证内容不丢不重。所以我把每个 chunk 都编号并缓存下来，前端再维护 `lastEventIndex`。重连后，服务端会根据 `sessionId` 找到这次回答，再从 `lastEventIndex + 1` 开始补发缺失 chunk，前端再按 `index` 去重，这样就能真正从断点继续。

### 2.4 1分钟性能优化回答

> 最开始我踩到的最大问题不是网络，而是渲染：如果每收到一个 token 就直接 `setState`，页面会频繁重渲染，滚动也会抖，长一点的回答就会明显卡。为了解决这个问题，我先加了一个缓冲区，把流式数据先放到 buffer 里，再按 200ms 批量刷新一次 UI。这样做之后，用户几乎感觉不到 100 到 200ms 的延迟，但渲染频率会大幅下降，页面稳定很多。这个阶段我没有直接上纯 `requestAnimationFrame`，因为当前更核心的问题是“先把刷新频率压下来”；如果后续再遇到更高频、更明显的帧同步需求，我会把调度进一步演进成 `rAF + 最小间隔控制`。

### 2.5 1分钟滚动与列表优化回答

> 流式聊天还有两个容易被忽略的问题：一个是自动滚动会不会打断用户看历史消息，另一个是历史消息变多后 DOM 会不会越来越重。针对第一个问题，我没有无脑每次都滚到底，而是根据用户是否在底部来决定是否自动跟随；如果用户正在看历史消息，我只给一个“有新消息，点击回到底部”的提示。针对第二个问题，我用了 `react-virtuoso` 做虚拟滚动，避免历史消息很多时整个列表都堆在 DOM 里。这样解决的是“长列表性能”问题，而不是通信问题。

### 2.6 高频速答

**Q：为什么不用 WebSocket？**

> 因为我最先遇到的问题不是双向通信不够，而是单向流式响应的稳定性和实现复杂度。这个场景下 WebSocket 是过度设计。

**Q：为什么后来不用原生 EventSource？**

> 因为真实问题暴露出来了：上下文和历史消息一长，GET URL 会过长，稳定性下降，所以我改成了 `fetch + POST`。

**Q：为什么不是每个 token 都立刻更新？**

> 因为我先遇到的是频繁重渲染导致卡顿，所以优先做了缓冲区批量刷新。

**Q：为什么要做虚拟滚动？**

> 因为我后来遇到的不是“单次刷新慢”，而是“历史消息一多，整个列表越来越重”，虚拟滚动解决的是长列表问题。

---

## 3. 当前项目实现概览

### 3.1 当前链路

当前链路可以概括成：

`用户输入 -> Zustand store -> OptimizedAIService -> POST /api/chat-stream -> Express -> DeepSeek流式接口 -> SSE数据块 -> 前端缓冲刷新 -> Virtuoso消息列表`

### 3.2 当前关键模块

- 前端流式服务：`src/ReactPlayground/services/optimizedAI.ts`
- 前端状态管理：`src/ReactPlayground/stores/aiStore.ts`
- 前端消息列表与滚动：`src/ReactPlayground/components/AIAssistant/index.tsx`
- 后端流式接口：`api/chat.cjs`

### 3.3 当前真实能力

当前你可以稳定对外讲的能力有：

- 基于 HTTP 的流式 AI 输出
- `fetch + POST + ReadableStream` 解析 SSE
- 缓冲区 200ms 批量刷新
- 自动重连
- 基于事件序号的断点续传
- 底部跟随滚动与新消息提示
- `react-virtuoso` 虚拟滚动

---

## 4. 选型：为什么选SSE

### 4.1 我先遇到的问题

一开始我并不是在 WebSocket、SSE、轮询之间“理论选型”，而是先看业务场景：

- 用户发一次请求
- 服务端持续返回生成中的文本
- 基本没有高频双向交互

这个场景天然更像“单向流式响应”，不是聊天室，也不是协同编辑。

### 4.2 所以为什么选SSE

因为它正好解决了我眼前的需求：

- 单向推送足够
- 基于 HTTP，实现轻
- 调试简单
- 服务端接入成本低

### 4.3 为什么不是轮询

如果用轮询，我会立刻遇到这些问题：

- 延迟高
- 请求开销大
- 没法做自然的打字机效果
- 体验明显不像真正流式输出

### 4.4 为什么不是WebSocket

WebSocket 并不是不能做，而是我当时没遇到“必须双向实时通信”的问题，反而更容易引入不必要的复杂度：

- 协议升级
- 双向连接维护
- 更复杂的连接状态管理

所以我的判断是：先用更贴场景的 SSE，把真正的问题解决掉。

### 4.5 面试话术

> 我选 SSE 不是因为它“听起来高级”，而是因为我先看到了问题类型。这个功能本质上是单向流式输出，不需要 WebSocket 的双向能力。SSE 更符合场景，也更轻量。后面真正暴露出来的问题其实不是“选没选 SSE”，而是“原生 EventSource 的 GET 方案在长上下文下不够稳”，所以我后面做的是连接方式的演进，而不是推翻整个 SSE 思路。

---

## 5. 从 EventSource + GET 到 fetch + POST

### 5.1 最初方案是什么

最早为了快速验证流式体验，我用了：

- 前端 `EventSource`
- 通过 GET 参数传 `message`、`context`、`conversationHistory`
- 后端返回 SSE 数据流

这一步的优点是：

- 上手快
- 协议直观
- 很容易验证“能不能流式吐字”

### 5.2 后来遇到了什么问题

随着上下文和历史消息越来越长，我遇到了一个真实问题：

- URL 被拼得越来越长
- 连续追问“继续”时更明显
- 浏览器或代理层可能直接拒绝请求
- 前端最终只能看到统一的 SSE 连接错误

也就是说，早期方案不是“不能用”，而是只适合短参数、短上下文的验证阶段。

### 5.3 所以我做了什么优化

我没有放弃 SSE，而是只改连接方式：

- 保留 SSE 数据格式
- 保留流式返回
- 前端从 `EventSource` 改成 `fetch + POST + ReadableStream`
- 参数从 URL 改到请求体

### 5.4 这样改完解决了什么

- 解决了 GET URL 过长的问题
- 请求参数承载能力更强
- 前端可控性更高，后面做重连和续传也更方便

### 5.5 面试话术

> 这个演进我觉得很值得讲，因为它不是空谈架构，而是被真实问题逼出来的。最早我为了快，用的是 `EventSource + GET`。后来随着上下文和历史消息变长，URL 过长开始导致 SSE 连接错误。这个时候我没有硬扛，而是保留 SSE 的流式语义，把连接层演进成 `fetch + POST + ReadableStream`。这个调整很小，但稳定性提升很明显。

---

## 6. 协议原理与当前实现

### 6.1 我遇到的实现问题

当我把连接方式从 `EventSource` 改成 `fetch` 之后，浏览器不再帮我自动解析事件了，所以新的问题变成：

- 如何自己读流
- 如何自己拆包
- 如何识别完成事件和错误事件

### 6.2 所以我怎么实现

后端仍然返回标准 SSE 文本流，比如：

```text
data: {"index":0,"content":"你好"}

data: {"index":1,"content":"，我来帮你分析"}

event: done
data: [DONE]
```

前端做的事情是：

1. `fetch` 发起请求
2. 从 `response.body.getReader()` 读取字节流
3. 用 `TextDecoder` 解码
4. 按换行拆分
5. 识别 `data: ...`
6. JSON 解析出 `{ index, content }`
7. 再交给去重器和缓冲区

### 6.3 这样做的价值

这一步解决的是“连接方式更灵活之后，如何保留流式语义”的问题。

### 6.4 面试话术

> 虽然前端不再用原生 EventSource，但我没有放弃 SSE 语义。后端照样返回 `text/event-stream`，只是前端不再依赖浏览器帮我解析，而是自己通过 `ReadableStream` 去读、拆包和处理。这样做的好处是灵活性更高，尤其适合我后面继续加重连、续传、状态提示这些能力。

---

## 7. 重连：为什么做，怎么做

### 7.1 我先遇到了什么问题

AI 回答一长，就很容易暴露网络问题：

- 中途断线
- 代理层连接被切
- 标签页切换或弱网抖动

如果这时没有重连，用户会直接遇到：

- 回答生成到一半失败
- 只能重问
- 长回答体验非常差

### 7.2 所以我做了什么优化

我做的是“有限次数自动重连”，不是无限重试：

- 最多 3 次
- 500ms、1000ms、2000ms 退避
- 用户主动取消时不重连
- session 明确失效或参数不一致时不重连

### 7.3 为什么这样设计

因为我想解决的是“瞬时波动恢复”，不是“无限自愈”。

如果无限重连，会带来新的问题：

- 放大服务端压力
- 更容易撞上限流
- 用户会以为页面一直卡住

### 7.4 做完之后的效果

- 短时网络波动时，大多数场景可以自动恢复
- 用户感知明显更好
- 不会把错误恢复做成新的稳定性问题

### 7.5 面试话术

> 我做重连是因为先遇到了“长回答过程中的中途断线”问题。这个场景下，如果直接失败，用户体验会很差。所以我加了有限次数的自动重连，用 500ms、1000ms、2000ms 退避。这样能覆盖大多数瞬时抖动，但又不会无限重试把系统拖垮。

---

## 8. 断点续传：为什么重连还不够

### 8.1 我又遇到了什么问题

只做重连还不够，因为我很快发现：

- 重新连上，不代表能从正确位置继续
- 可能重复生成
- 可能丢掉中间一段

也就是说，重连解决的是“重新连上”，但没有解决“从哪里接着来”。

### 8.2 所以我做了什么优化

我引入了两个字段：

- `sessionId`：标识是哪一条回答
- `lastEventIndex`：标识已经收到第几段了

同时在服务端为每条回答维护 session，缓存：

- 原始请求参数
- 已生成 chunk
- chunk 的递增序号
- 当前状态
- 当前活跃连接

### 8.3 续传流程

1. 首次请求生成 `sessionId`
2. 上游每产出一段内容，后端给它一个递增 `index`
3. 前端收到后更新 `lastEventIndex`
4. 断线后，前端带着 `sessionId + lastEventIndex` 重连
5. 后端先补发 `index > lastEventIndex` 的 chunk
6. 如果上游还没结束，再继续推实时内容

### 8.4 我为什么还在前端做去重

因为这里有一个边界问题：

- 前端可能已经收到某个 chunk
- 但还没来得及更新 `lastEventIndex`
- 这时连接就断了

这样重连后最后一个 chunk 可能被补发一次。

所以我又做了一层按 `index` 去重，避免重复拼接。

### 8.5 做完之后的效果

- 重连不再是“整段重来”
- 可以从断点继续
- 内容既不容易丢，也不容易重复

### 8.6 面试话术

> 我后来发现重连本身不难，真正难的是重连后怎么保证内容从正确位置继续。所以我把“重连”和“续传”拆开看：前者解决重新建立连接，后者解决从哪里继续。我用 `sessionId` 标识会话，用 `lastEventIndex` 标识断点，服务端缓存 chunk，前端按 `index` 去重，这样才算真正把续传做完整。

---

## 9. 流式输出中的中断与异常处理

### 9.1 我先遇到了什么问题

流式输出真正难的不是“把流接出来”，而是“流在任何时刻中断时，系统还能不能停在一个合理状态”。

一旦中断处理没设计好，就很容易出现这些问题：

- 用户点击停止后，请求还在后台继续跑
- 页面卸载后，流还在向已销毁组件推数据
- 网络断开后，消息一直停留在“生成中”
- 已经生成的内容被清空，用户只能整段重来
- 重连和重复请求叠在一起，状态越来越乱

所以这块我不是把中断当成边缘情况，而是当成流式渲染里的常态路径来设计。

### 9.2 所以我先做了什么工程区分

我先把中断来源拆成两类：

**1. 用户主动中断**

- 点击“停止生成”
- 切换会话
- 页面卸载 / 路由切换
- 发起新的提问覆盖当前生成

**2. 非预期异常中断**

- `fetch` 失败
- `reader.read()` 抛出异常
- 流提前关闭但没有正常完成标志
- 数据解析失败
- 长时间没有新的 token
- 网络抖动或服务端中止连接

这样做是因为如果不先区分来源，后面就很容易把所有中断都粗暴当成“用户取消”，导致消息状态和 UI 语义都不准确。

### 9.3 为什么要把流生命周期和消息生命周期解耦

我这里有一个很关键的工程判断：

> 流怎么结束，不应该直接等于消息是什么状态。

因为流层只说明“连接发生了什么”，但 UI 层真正关心的是：

- 这条消息是正常完成了
- 还是用户主动停掉了
- 还是系统异常打断了

所以我会显式定义消息终态：

- `generating`：生成中
- `completed`：正常完成
- `aborted`：用户主动中断
- `failed`：异常中断

这样无论底层流是正常结束、被 abort，还是异常断开，消息最终都能进入一个确定状态，避免界面一直悬挂在“生成中”。

### 9.4 主动中断是怎么做的

针对用户主动中断，我会把它当成一次完整的流回收，而不是单纯停掉网络请求。

当前实现里，流层是通过 `AbortController` 控制的，所以在下面这些场景里，我都会立即执行中断：

- 用户点击停止
- 新请求覆盖旧请求
- 页面卸载
- 路由切换

中断后会同步做这些收敛动作：

- 停止继续读取流
- 清理缓冲刷新相关逻辑
- 释放当前 reader / signal 引用
- 恢复输入状态
- 把当前消息收敛为 `aborted`

这样做解决的是“用户已经不想要这次生成了，但系统还在继续跑”的问题。

### 9.5 异常中断是怎么兜底的

除了主动中断，另一类问题是系统异常。

比如我会专门处理这些入口：

- `fetch` 请求失败
- `reader.read()` 抛错
- 流提前结束但没有正常完成信号
- 数据解析失败

一旦判断为异常中断，我会统一做几件事：

- 立即停止继续消费流
- 保留已经生成的文本内容
- 将消息状态标记为 `failed`
- 释放流相关资源
- 恢复用户输入能力

这里一个很重要的原则是：

> 异常不会清空已生成内容。

因为对用户来说，最差体验不是“报错”，而是“已经生成了一半，结果全没了”。所以即使失败，也要保留部分成功结果。

### 9.6 长时间无 token 的前端兜底

我还会考虑一种很容易被忽略的情况：

- 连接没断
- 但模型长时间不再返回新 token
- 页面却一直显示“生成中”

这种场景下，我会在前端做基于时间的兜底：

- 记录最近一次 token 到达时间
- 超过阈值后主动中断当前流
- 将本次消息收敛为 `failed`

这个策略不是替后端判断业务成功失败，而是为了防止前端状态被无限期拖住。

### 9.7 自动重连怎么和异常处理配合

当我把异常中断识别出来之后，不会立刻一刀切失败，而是先判断这次异常是否值得恢复。

所以当前策略是：

- 用户主动中断：直接结束，不再重连
- 异常中断：进入自动重连流程

重连时会带上：

- `sessionId`
- `lastEventIndex`

这样服务端才能从断点继续补发，而不是整段重新开始。

这里最关键的是：

- 中断作用在流层
- 收敛发生在状态层

也就是说，流先停下来，再由状态层判断它最终应该落到 `aborted`、`failed` 还是恢复继续。

### 9.8 页面卸载与路由切换为什么一定要管

这块很多人容易漏，但在流式场景里非常关键。

如果页面卸载或路由切换时不统一中断流，很容易出现：

- 已卸载组件继续收流
- 状态更新打到无效组件
- 内存泄漏
- 控制台 warning 或异常

所以页面生命周期和流生命周期必须保持一致。页面销毁时统一 abort，本质上是防止系统把旧流带到无效界面里。

### 9.9 面试话术

> 在流式对话设计里，我没有把中断当成偶发现象，而是把它当成必备能力来设计。因为 LLM 流式生成天然存在不确定性，前端不能假设流一定完整结束。所以我先在工程上区分了两类中断：用户主动中断和系统异常中断。前者通过 `AbortController` 立即停止当前流，并把消息状态收敛为 `aborted`；后者则会保留已生成内容，结合超时、异常捕获和重连策略，把消息最终收敛为 `failed` 或恢复继续生成。整个设计里最核心的判断是：流的结束方式，不直接决定消息的最终状态，消息必须始终进入 `generating / completed / aborted / failed` 之一。这样系统在弱网、长文本生成和页面切换场景下，才能保持稳定和可控。

### 9.10 结论句

> 流式输出的难点不在于“能不能接到流”，而在于“任何时候断掉，都能停在一个合理状态”。

---

## 10. 渲染优化：为什么先用200ms缓冲，再谈rAF

### 9.1 我先遇到了什么问题

最开始的流式渲染如果写得直接一点，通常会这样：

- 每收到一个 token
- 就立刻更新一次 React 状态

这会立刻带来两个问题：

- 重渲染频率极高
- 滚动和布局不断被打断

表现出来就是：

- 页面发卡
- 输入和滚动不够流畅
- 回答一长就明显抖

### 9.2 所以我第一步做了什么优化

我先加了一个缓冲区：

- 先把 token 放进 `buffer`
- 按 200ms 批量刷新一次 UI
- 流结束时再做一次最终刷新

### 9.3 为什么我先这么做

因为我当时遇到的核心问题不是“帧调度不够优雅”，而是“刷新次数太多”。

先用 `200ms setTimeout` 的好处是：

- 实现简单
- 节流效果稳定
- 能快速验证性能收益

### 9.4 后续为什么会考虑rAF

当“刷新次数太多”这个问题先被压住之后，下一步可能会遇到更细的体验问题：

- 某些高频场景下，刷新和浏览器绘制节奏不够贴合
- 有时候会想进一步追求更平滑的逐帧感受

这时就可以进一步把调度从“固定时间片”演进成：

- `buffer + requestAnimationFrame`
- 或者更稳一点：`buffer + rAF + 最小间隔控制`

### 9.5 这里该怎么讲才严谨

如果你当前代码主体还是 200ms 批量刷新，那面试里不要说：

> 我已经从 setTimeout 改成了 rAF

更准确的说法是：

> 我先遇到了高频重渲染导致卡顿的问题，所以第一步先用 200ms 缓冲区批量刷新，把刷新频率压下来。等这个问题解决之后，如果后续还要继续追求更平滑的帧同步效果，再把调度进一步演进到 `requestAnimationFrame`。

### 9.6 面试话术

> 我这里是分阶段优化的。最早遇到的问题是每个 token 都触发 React 更新，导致渲染频率太高、页面卡。所以第一步我先上了缓冲区和 200ms 批量刷新，因为它简单、稳定、节流效果明确。等这类问题先解决之后，如果后续还遇到更细的帧同步需求，我会再把刷新调度演进到 `requestAnimationFrame`。也就是说，rAF 不是和缓冲区冲突，而是下一步调度层优化。

### 9.7 高频追问

**Q：`requestAnimationFrame` 和缓冲区冲突吗？**

> 不冲突。缓冲区解决的是“先聚合数据”，rAF 解决的是“什么时候刷新更合适”。一个是数据层，一个是调度层。

**Q：为什么现在不用纯rAF？**

> 因为我当前先遇到的是“刷新太频繁”问题，所以优先做了固定时间片节流。纯 rAF 更偏帧同步，如果不叠加最小间隔控制，也可能仍然太频繁。

---

## 11. 滚动策略：为什么不能无脑滚到底

### 10.1 我先遇到了什么问题

流式聊天里如果每次有新内容都强制滚到底，会出现一个很真实的问题：

- 用户刚往上翻历史消息
- 新 token 一来
- 列表又被强行拉回到底部

这对用户来说非常打扰。

### 10.2 所以我做了什么优化

我把滚动逻辑拆成了两种状态：

- 用户在底部：自动跟随输出
- 用户不在底部：停止自动跟随，只显示“有新消息”提示

具体实现是：

- 用 `atBottomStateChange` 判断是否在底部
- 用 `followOutput` 控制是否跟随
- 用按钮提示用户手动回到底部

### 10.3 做完之后的效果

- 用户实时看输出时体验流畅
- 用户查看历史消息时不会被打断
- 滚动条不再抖

### 10.4 面试话术

> 我在滚动上遇到的问题不是“不会滚”，而是“滚得太自作主张”。如果无脑每次都滚到底，会严重影响用户看历史消息。所以我加了一个底部状态判断：用户在底部时，系统自动跟随输出；用户不在底部时，系统不强行滚动，只给一个“有新消息”的提示。这样交互更合理。

---

## 12. 虚拟滚动：为什么长聊天要单独优化

### 11.1 我后来又遇到了什么问题

即使把单次渲染频率压下来了，历史消息一多，还是会出现另一个问题：

- DOM 节点越来越多
- 重排和重绘成本越来越高
- 长时间聊天后列表会变重

也就是说，前面的缓冲区优化解决的是“高频更新”，但没解决“长列表积累”。

### 11.2 所以我做了什么优化

我用了 `react-virtuoso` 做虚拟滚动。

它解决的是：

- 只保留可视区域附近的 DOM
- 历史消息再长，DOM 数量也不会线性增长

### 11.3 为什么不用自己手写

因为这里我遇到的问题已经不是“原理不会”，而是“工程上值不值得自己造轮子”。

项目重点在于：

- 流式协议
- 重连
- 续传
- 渲染稳定性

虚拟滚动已经有成熟方案，我更愿意把精力放在前面那些更核心的问题上。

### 11.4 面试话术

> 我把性能问题拆成了两类：一类是高频 token 更新导致的短时渲染压力，这个我用缓冲区批量刷新解决；另一类是历史消息越来越多导致的长列表压力，这个我用 `react-virtuoso` 的虚拟滚动解决。也就是说，虚拟滚动不是为了 SSE 本身，而是为了长时间聊天后的整体性能稳定性。

---

## 13. 高频面试题与回答

### 12.1 为什么选SSE而不是WebSocket？

> 因为我先看到的问题是“单向流式输出怎么做得简单稳定”，而不是“双向实时通信不够”。AI 助手这个场景里，SSE 就够了。

### 12.2 为什么从EventSource换成fetch？

> 因为我先遇到了长上下文下 URL 过长的问题。`EventSource + GET` 在早期验证阶段很好用，但在真实聊天场景下不够稳，所以我把连接方式演进成了 `fetch + POST`。

### 12.3 为什么要做缓冲区？

> 因为我先遇到了每个 token 都更新导致的高频重渲染和卡顿，所以先用缓冲区把刷新频率压下来。

### 12.4 为什么要做重连？

> 因为我先遇到了长回答过程中中途断线的问题，如果不重连，用户只能整段重来。

### 12.5 为什么重连还不够，还要断点续传？

> 因为我后来发现仅仅重连只能“重新连上”，不能保证“从正确位置继续”，所以才引入了 `sessionId + lastEventIndex + chunk index` 这一套机制。

### 12.6 为什么要做滚动控制？

> 因为我遇到的是用户看历史消息时被强制拉回底部的问题，所以不能无脑自动滚动。

### 12.7 为什么要做虚拟滚动？

> 因为我后来又遇到了长列表 DOM 过重的问题，虚拟滚动解决的是长期累计压力。

### 12.8 `requestAnimationFrame` 和 `setTimeout` 怎么取舍？

> 我是按问题分阶段处理的。先遇到的是“刷新频率太高”，所以先用 200ms `setTimeout` 做缓冲区批量刷新；如果后续再遇到更细的帧同步问题，再把调度层演进到 `requestAnimationFrame`。

---

## 14. 可直接背诵的完整回答

### 13.1 题目：详细讲讲你这个SSE AI助手是怎么做的

> 这个功能不是一步做到位的，而是被一连串真实问题推动出来的。最开始我只是想做一个流式 AI 助手，所以先用了 `EventSource + GET`，很快把 SSE 流接通了。但随着上下文、历史消息和文件内容变长，我遇到了第一个真实问题：URL 过长导致连接稳定性变差。所以我把连接方式演进成了 `fetch + POST + ReadableStream`，保留 SSE 数据格式，但把参数放进 body。接着我又遇到了第二个问题：如果每个 token 都直接更新 React，页面会明显卡顿。所以我加了缓冲区，把数据先放到 buffer 里，再按 200ms 批量刷新 UI。然后我又遇到了第三个问题：长回答过程中中途断线后，单纯重连并不能保证内容正确继续，所以我又补了 `sessionId + lastEventIndex` 的断点续传机制，服务端缓存 chunk，前端按 `index` 去重。最后，在交互层面我还遇到了两个体验问题：一个是无脑自动滚动会打断用户看历史消息，另一个是历史消息变多后 DOM 会越来越重。所以我又加了底部状态判断和 `react-virtuoso` 虚拟滚动。整个功能最后不是单点优化，而是一套围绕稳定性、性能和体验逐步演进出来的方案。

### 13.2 题目：讲讲你做的最有含金量的优化

> 我觉得最有含金量的是我没有把“重连”和“续传”混为一谈。最开始我先遇到的是长回答中途断线，这促使我加了自动重连。但很快我又发现，重连只能解决重新建立连接，不能保证内容不丢不重。所以我进一步把服务端每个 chunk 编号并缓存起来，前端维护 `lastEventIndex`，重连时按断点补发，前端再按 `index` 去重。这个优化的价值在于，它不是简单把请求再发一次，而是真正把长流式响应在弱网下的可恢复性做好了。

---

## 15. 最后提醒

### 14.1 可以稳讲的

- 为什么选 SSE
- 为什么从 EventSource 演进到 fetch + POST
- 为什么先做 200ms 缓冲区批量刷新
- 为什么重连之后还要做断点续传
- 为什么自动滚动不能无脑到底
- 为什么长聊天一定要考虑虚拟滚动

### 14.2 要谨慎讲的

- `requestAnimationFrame`
  你可以讲成后续优化方向，但如果当前代码主体还是 200ms 批量刷新，就不要说成“已经全面改成 rAF”
- Redis 持久化续传
  当前项目主体还是内存 session，不要讲成线上持久化架构
- 完全不会失败
  当前只是有限次数自动恢复，不是无限自愈

### 14.3 一个很重要的表达原则

面试时尽量按这个顺序讲：

1. 我先遇到了什么问题
2. 为什么会这样
3. 我做了什么优化
4. 解决了什么
5. 还有什么后续演进空间

这样最像真实做过项目的人。
